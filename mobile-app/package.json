{
  "name": "4s-orders-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android (APK) wrapper for the 4S Interiors Orders app. Isolated from the production web frontend/backend.",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
